/**
 * _abs - checks the absolute value of an integer
 *@n: number being checked
 * Return: 0 (Success);
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
	return (0);
}
